<% provide(:title, 'All User')%>
<h1>All Users</h1>

<%= will_paginate @users, :previous_label => 'Sebelumnya', :next_label => 'Selanjutnya' %>

<!-- <ul class="users">
	<%# @users.each do |user| %>
		<%#= render user %>
	<%# end %>
</ul> -->
<ul class="users">
	<%= render @users %>
</ul>

<%= will_paginate @users, :previous_label => 'Sebelumnya', :next_label => 'Selanjutnya' %>